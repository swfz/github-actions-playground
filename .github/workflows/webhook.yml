name: slack

on: push

jobs:
  incomming_webhook:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2.3.2
      - run: ls hoge
      - name: success
        if: success()
        run: echo 'success'
      - name: failure
        if: failure()
        run: echo 'failure'


